

## Análise

A rota `/clientes-tarefas` já foi removida do `src/App.tsx` no último diff. Verificando se ainda há referências residuais no código.

Buscando referências a `clientes-tarefas` e ao componente `Clientes` (a página legada).

