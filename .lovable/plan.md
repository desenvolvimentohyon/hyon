

## Aumentar logo na tela de login

Alterar a classe CSS da imagem do logo em `src/pages/Auth.tsx` de `h-16` (~64px) para `h-28` (~112px, aproximadamente 70% maior).

**Arquivo:** `src/pages/Auth.tsx`
- Linha com `className="h-16 w-auto"` → mudar para `className="h-28 w-auto"`

